{
  "name": "redis-streams-cleaner",
  "version": "1.0.3",
  "description": "Clean Redis Streams messages that are were already read by all consumer groups",
  "repository": {
    "url": "https://github.com/marcelo-hexsel/redis-streams-cleaner"
  },
  "homepage": "https://github.com/marcelo-hexsel/redis-streams-cleaner",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "lint": "eslint src/ --ext .js,.jsx,.ts,.tsx",
    "test": "yarn lint && jest --no-cache",
    "functional-test": "jest -c jest.config.functional.js --runInBand --force-exit",
    "build": "tsc"
  },
  "author": "Marcelo Fleck Hexsel",
  "license": "MIT",
  "dependencies": {
    "redis": "^3.1.2"
  },
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "@types/redis": "^2.8.31",
    "@typescript-eslint/eslint-plugin": "^4.19.0",
    "@typescript-eslint/parser": "^4.19.0",
    "eslint": "^7.22.0",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.5",
    "ts-node": "^9.1.1",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.3.5"
  }
}
